<?xml version="1.0" encoding="utf-8"?>
<!-- This AndroidManifest.xml template was generated by Skip -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- example permissions for using device location -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> -->

    <!-- permissions needed for using the internet or an embedded WebKit browser -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <!-- permissions needed for foreground media playback service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Required by ExoPlayer's setWakeMode(WAKE_MODE_NETWORK) in SharedAudioPlayer: holds a
         PowerManager + WifiLock while playing so a backgrounded/car session isn't stalled by doze.
         media3 holds the locks only while READY/BUFFERING with playWhenReady. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required on Android 13+ (API 33) for the media notification to appear in the
         notification drawer and lock screen. Without this, the foreground service still runs
         but the notification is suppressed from user-visible surfaces. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- TV support: leanback UI available but not required; no touchscreen required, so the one
         app installs on both phones and TVs. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="${PRODUCT_NAME}"
        android:name=".AndroidAppMain"
        android:supportsRtl="true"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:theme="@style/Theme.Maxi80"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:theme="@style/Theme.Maxi80"
            android:windowSoftInputMode="adjustResize"><!--
            Rotation is unlocked since the state-driven CoverFlowStrip (2026-08-01): the carousel
            renders from CarouselModel.selectedID by pure index math, so a rotation relayout — or
            a full activity recreation — re-derives the centered cover instead of preserving a
            scroll offset. The previous `nosensor` lock existed only because the old ScrollView
            renderer's offset did not survive relayout. -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="maxi80.services.Maxi80MediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- FileProvider exposing cached track artwork as a content:// URI for the native share
             chooser (Intent.ACTION_SEND). A raw file:// URI would throw FileUriExposedException on
             API 24+. Authority and paths must match AndroidShareService. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Android Auto support -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
    </application>
</manifest>
